/**
 * Placeholder root route.
 * This exists only so the Next.js app boots after initialization (CORE-001).
 * Replace via an approved task (e.g., CORE-00X: Landing Page / Dashboard).
 */
export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-sm text-muted-foreground">mCORE — project initialized.</p>
    </main>
  );
}
